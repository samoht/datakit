(jbuild_version 1)

(executable
 ((name        test)
  (package     datakit)
  (libraries   (alcotest lwt datakit-client datakit-server mtime.os
                datakit_io datakit datakit_conduit logs.fmt))
 ))

(alias
 ((name runtest)
  (deps (test.exe))
  (action (run ${exe:test.exe} -q))))